import React from "react";
import AppLayout from "../components/layout/AppLayout";
import { Typography } from "@mui/material";
// import AppLayout from "../components/layout/AppLayout";

const Home = () => {
  return <Typography >Select a friend to Chat</Typography>;
};

export default AppLayout()(Home);
